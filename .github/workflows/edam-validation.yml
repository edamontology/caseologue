name: EDAM-VALIDATION 

on: [workflow_dispatch]

jobs:
  
  job1_edamci:
    
      runs-on: ubuntu-latest

      steps:

        - uses: edamontology/edam-validation/.github/workflows/run_edamci.yml
