
name: test caseologue

on: 
  push:

jobs:
  
  build:

    runs-on: ubuntu-latest

    steps:
      
    - uses: actions/checkout@v2

    - name: Install requirements
      run: |
        pip install -r caseologue_python/requirements.txt
    - name: run test_caseologue
      if: always()
      run: |
        cd caseologue_python/test/
        echo "PYTHONPATH=${PYTHONPATH}:/home/runner/work/caseologue/caseologue_python" >> $GITHUB_ENV
        python3 test_caseologue.py



# duplicate all not tested, too long to run
# export PYTHONPATH="${PYTHONPATH}:/home/runner/work/caseologue/caseologue_python"
